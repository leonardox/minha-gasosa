<div class="row">
    <div class="col-md-offset-4 col-md-4">
        {{#if error}}
            <div class="alert alert-danger">{{error.message}}</div>
        {{/if}}
        <form class="form-login" method="post" action="authenticate">
            <input type="text" name="username" class="form-control input-sm" placeholder="Username" />
            <input type="password" name="password" class="form-control input-sm" placeholder="Password" />
            <button type="submit" class="form-control btn btn-primary btn-md">Login <i class="fa fa-sign-in"></i></a>
        </form>
    </div>
</div>

<style>
    .alert {
        margin: 12pt;
        margin-left: 22pt;
    }
    .form-control {
        margin: 6pt;
    }
</style>